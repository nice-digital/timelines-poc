import React, { Component } from 'react';

export class Home extends Component {
  displayName = Home.name

  render() {
    return (
      <div>
        <h1>Timelines alpha proof of concept</h1>
        <p>Contains proof of concept pages for the timelines alpha including reporting, and the cross institute planner</p>
      </div>
    );
  }
}
